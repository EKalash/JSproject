"use strict";
let number = 5;
 const person = 'Alex'; // const - константа не изменяется
 number = 10;
 console.log (number);
 const bool = false; 
 console.log (something); // ReferenceError: something is not defined - это значение переменной null, ошибка ссылочного типа, мы не можем сослаться  на существующий объект

 let und;
 console.log (und); // undefined - не присв

 const obj = { // создание объекта через фигурные скобки
     name: "ALEX",
     age: 40,
     isMArried: true
 };
 console.log (obj.name);

 let arr = ['name', 4, {}]; // создание массива
 console.log (arr[2]);
 
